# Docs history cleanup (Phase H)

## Goal

Keep the repo front door free of stale `FINAL-*` / `FIXES-*` / `*-COMPLETE*` notes
so contributors land on advancement + architecture + contracts first.

## Action

1. Create `docs/history/` with an index README.
2. Move historical status / fix / preset-completion notes into `docs/history/`.
3. Point `docs/development/README.md` at the archive explicitly.
4. Do **not** rewrite or delete content in this pass — relocate only.

## Exit

- Root no longer hosts FINAL/FIXES completion dumps
- Advancement Phase H marked PASS
